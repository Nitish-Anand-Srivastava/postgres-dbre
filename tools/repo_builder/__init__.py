"""Source generator package for the Aurora PostgreSQL DBA Toolkit.

This package is the single source of truth used by tools/build_repository.py
to render the repository tree (issue workflow directories, READMEs, and SQL
scripts). Editing generated files directly is discouraged -- change the data
in this package and re-run the builder instead.
"""
